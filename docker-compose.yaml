name: cloudflared

services:
  cloudflared:
  container_name: cloudflared
  image: cloudflare/cloudflared:latest
  command: tunnel --no-autoupdate run
  env_file:
    - C:/Users/Lu/Server/Storage/cloudflared/.env
  restart: unless-stopped
